// This code was entirely generated by ChatGPT, I just copied it and changed some of the variable names.

// Includes
// General cpp libraries
#include <fstream>
#include <iostream>
#include <memory>

// ROS2 specific libraries
#include "rclcpp/rclcpp.hpp"
#include "rclcpp/serialization.hpp"
#include "rosbag2_cpp/reader.hpp"
#include "rosbag2_storage/storage_options.hpp"
#include "rosbag2_cpp/converter_options.hpp"

// Messages
#include "sensor_msgs/msg/imu.hpp"

int main (int argc, char ** argv) {

    // Check that the number of arguments is correct
    if (argc != 3) {
        std::cerr << "Expected 3x arguments: ros2 run tool imu_data_to_csv <rosbag_path> <csv_imu_file_path>" << std::endl;
        return 1;
    }

    // Get rosbag path and CSV file path
    const std::string bag_path = argv[1];
    const std::string csv_path = argv[2];

    // Open CSV file
    std::ofstream csv(csv_path);

    // Check that CSV file was opened
    if (!csv.is_open()) {
        std::cerr << "Failed to open CSV" << std::endl;
        return 1;
    }

    // Add headers to CSV file
    csv << "seconds, nanoseconds \n";

    // Open rosbag
    rosbag2_cpp::Reader reader;
    rosbag2_storage::StorageOptions storage_options;
    storage_options.uri = bag_path;
    storage_options.storage_id = "sqlite3";
    rosbag2_cpp::ConverterOptions converter_options;
    converter_options.input_serialization_format = "cdr";
    converter_options.output_serialization_format = "cdr";
    reader.open(storage_options, converter_options);

    // Serialization object
    rclcpp::Serialization<sensor_msgs::msg::Imu> serialization;

    while (reader.has_next()) {
        auto bag_msg = reader.read_next();

        // Ignore all topics except "imu_and_timestamps"
        if (bag_msg->topic_name != "/imu_and_timestamps") {
            continue;
        }

        // Deserialize
        rclcpp::SerializedMessage serialized_msg(*bag_msg->serialized_data);
        sensor_msgs::msg::Imu msg;

        serialization.deserialize_message(
            &serialized_msg,
            &msg);

        // Write to CSV
        csv << msg.header.stamp.sec << "," << msg.header.stamp.nanosec << "\n";

    }

    // Close CSV
    csv.close();

    // Notify of completion
    std::cout << "Finished writing " << csv_path << "\n";

    return 0;

}