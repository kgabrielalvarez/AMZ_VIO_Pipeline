// Move state enum to this header file and then include it in 
// the other header files!!!!!!!!!!!!!!!!

// Also add state_to_string function here

// Probably also makes sense to add state transition handler here but that's probably hard to do since it needs to be a method you would need to create a new class or object
// or whatever it's called and inherit from it for the other nodes